<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <style>
    body {
      font-family: 'Segoe UI', Helvetica, Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #181926;
      color: #cad3f5;
    }

    .email-wrapper {
      width: 100%;
      padding: 40px 0;
      display: flex;
      justify-content: center;
    }

    .container {
      max-width: 500px;
      width: 90%;
      margin: 0 auto;
      background-color: #24273a;
      /* Base */
      border-radius: 20px;
      border: 1px solid #363a4f;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
      overflow: hidden;
    }

    .content {
      padding: 45px 35px;
      text-align: center;
    }

    h1 {
      color: #c6a0f6;
      /* Mauve */
      font-size: 28px;
      font-weight: 700;
      margin-top: 0;
      margin-bottom: 24px;
    }

    p {
      font-size: 16px;
      line-height: 1.7;
      color: #b8c0e0;
      /* Subtext 1 */
      margin-bottom: 25px;
    }

    .button-wrapper {
      margin: 35px 0;
    }

    .btn {
      background-color: #a6da95;
      /* Green */
      color: #181926 !important;
      /* Crust for contrast */
      padding: 16px 32px;
      text-decoration: none;
      border-radius: 12px;
      font-weight: 700;
      font-size: 16px;
      display: inline-block;
      box-shadow: 0 4px 15px rgba(166, 218, 149, 0.2);
    }

    .footer {
      margin-top: 30px;
      padding-top: 25px;
      border-top: 1px solid #363a4f;
      font-size: 13px;
      color: #6e738d;
      /* Overlay 0 */
    }

    .note {
      font-size: 12px;
      color: #5b6078;
      /* Surface 1 */
      margin-top: 20px;
    }
  </style>
</head>

<body>
  <div class="email-wrapper">
    <div class="container">
      <div class="content">
        <h1>{{title}}</h1>

        <p>{{content}}</p>

        {{#if link}}
        <p style="color: #cad3f5;">Please click the button below to continue:</p>
        <div class="button-wrapper">
          <a href="{{link}}" class="btn">
            Reset My Password
          </a>
        </div>
        {{/if}}

        <div class="footer">
          Best regards,<br>
          <strong>The Spot Team</strong>
        </div>

        <div class="note">
          If you didn't request this email, you can safely ignore it.
        </div>
      </div>
    </div>
  </div>
</body>

</html>